[package]
name = "lector-ghostty"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false
links = "ghostty-vt"
build = "build.rs"

[lib]
path = "src/lib.rs"
test = false
doctest = false

[dependencies]
serde = { version = "1.0.203", features = ["derive", "rc"] }
vte = "0.15.0"
