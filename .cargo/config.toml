[alias]
ghostty-bootstrap = "run --locked --package lector-xtask -- ghostty-bootstrap"
ghostty-check = "run --locked --package lector-xtask -- ghostty-check"
ghostty-bench = "run --locked --package lector-xtask -- ghostty-bench"
